<%= simple_form_for @statement, :url => statements_withdrawal_new_path, :method => :post do |f| %>
  <%= f.number_field :withdrawal %>
  <%= f.button :submit, "Submit", class: 'btn-success btn-block' %>
<% end %>
